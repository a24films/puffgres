- are `remove`, `reset`, and `dry-run` good abstractions? any way to make them better? can we just do these easily in postgres? or: is it worth simplifying these to literally just run state commands more easily? idk what do you think, i want to simplify API 
- fix `configuration.md` files and make the spec better. can we merge environment and configuration and are there some we could get rid of / some way to simplify? are we even using all of these? or is there some way to make there be fewer knobs to control 
- does mdbook have some way to create a skill / claude mdbook thing? like, can we have a cargo command that will generate the mdbook thing AND that will generate a full agents.md generated file that others can use? and maybe some sort of CURL command in the repo to instruct agents to use it?
- is there a non-interactive codepath that an agent could use rather than the CLI for the `new` command? or can you make the CLI take arguments? thsis should be in the skill file 
- is there some way to make the configuration/environment variables stuff simpler? there's a lot of variables - are we even still using all of them? and could you organize them better?
- does puffgres check run within the run loop? if not, it should, automatically
- remove transaction sub-batching maybe? or how much complexity does this add? seems like cruft that makes it very confusing. i'll need to fuzz / bench again but thats fine 
- fix the sections that i got rid of in the summary. 
- given that we only support the public schema for now, why not just get rid of it from transforms and not read it? - **`source.schema`** — the Postgres schema. Right now, only `public` is supported. or is this legit? 
- is creating your first config sufficiently simple? 
- how do i publish a docker image with the source vs. having these generally? 