[package]
name = "state"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.44", features = ["serde"] }
rusqlite = { version = "0.38.0", features = ["bundled"] }
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3.24.0"
